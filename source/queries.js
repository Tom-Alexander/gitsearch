

function search() {

}

function query(repository, path, type, file_type, extension, content) {
    // repo=''&path=''&type=''&filter_type=''&file_type&=''&extension=''&query=''&page='1'
    // if repository => must match repository
    // if path => must match path
    // if type => must match type
    // if file_type => must match file_type
    // if extension => must match extension
    // fuzzy match content
    // page, items_per_age
    //
}
